import { CommandLinePipe } from './command-line.pipe';

describe('CommandLinePipe', () => {
  it('create an instance', () => {
    const pipe = new CommandLinePipe();
    expect(pipe).toBeTruthy();
  });
});
